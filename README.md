# Math Ops (MLOps Addition Package)

A simple Python package demonstrating MLOps best practices for packaging, modularization, and version control.

## Project Structure
- `src/math_ops`: Contains the core logic for mathematical operations.
- `tests/`: (Pending) Unit tests for package verification.
- `pyproject.toml`: Build system configuration.

## Installation
To install the package in editable mode for development:
```bash
pip install -e .
