[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "math_ops"
version = "0.1.0"
description = "A simple MLOps addition project"

[tool.setuptools.packages.find]
where = ["src"]
